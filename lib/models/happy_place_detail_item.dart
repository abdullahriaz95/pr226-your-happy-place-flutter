import 'package:flutter/material.dart';

class HappyPlaceDetailItem {
  int id;
  String itemName;
  Color backgroundColor;
  HappyPlaceDetailItem({
    required this.id,
    required this.itemName,
    required this.backgroundColor,
  });
}
